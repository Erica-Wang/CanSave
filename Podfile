# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SHAD Frontend' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SHAD Frontend

	pod ‘Firebase’
	pod ‘FirebaseAuth’
	pod ‘Firebase/Database’

  target 'SHAD FrontendTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SHAD FrontendUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
